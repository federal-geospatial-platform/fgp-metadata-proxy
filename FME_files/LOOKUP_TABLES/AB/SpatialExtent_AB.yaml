CSV_COLUMNS:
  id: [trim, lower]
  coor_valid: [trim, lower]
  west_bound_longitude_valid: [trim]
  east_bound_longitude_valid: [trim]
  north_bound_latitude_valid: [trim]
  south_bound_latitude_valid: [trim]
#
CREATE_KEY_VALUE:
#
NO_DUPLICATE:
  DUP_1: [id]
#
CHECK_DOMAIN:
